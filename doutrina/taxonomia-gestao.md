# Taxonomia de Gestão — 3 categorias (nunca colapsadas)

<!-- TODO: migrar taxonomia de jarvis-os ou do doc Drive "taxonomia-e-contexto". -->

> **PENDENTE — conteúdo não migrado.** O handoff do scaffold instrui a migrar a
> taxonomia das "3 categorias de gestão, nunca colapsadas" a partir do `jarvis-os` ou
> do documento do Drive **"taxonomia-e-contexto"**. Uma varredura do `jarvis-os`
> (`.md`) **não localizou** essa taxonomia escrita nesta sessão.
>
> Conforme a regra do handoff (passo 3): **não inventar as 3 categorias.** Este
> arquivo fica como stub até a fonte real ser fornecida.

## O que falta

- As **3 categorias de gestão** (nomes exatos + definição de cada uma).
- O critério que as mantém separadas ("nunca colapsadas") — por que colapsá-las é erro.
- Exemplos de aplicação em frentes/estudos.

## Como resolver

1. Fornecer o doc do Drive **"taxonomia-e-contexto"**, ou
2. Apontar o arquivo do `jarvis-os` onde a taxonomia vive (se existir em outra branch
   ou fora do escopo varrido), ou
3. Ditar as 3 categorias em conversa para registro aqui.

Assim que a fonte chegar, substituir este stub pelo conteúdo real.
